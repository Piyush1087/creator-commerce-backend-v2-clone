# Backend Docs

Use this folder for backend-specific operational notes.

Start with the root `RUNBOOK.md` before using these docs. The runbook is the
main tracker for current status, temporary work, next tasks, and doc references.

Suggested structure:

- `deployment` for SST, ECS, domains, profiles, and cutover notes.
- `database` for Prisma, migrations, local Postgres, and RDS/tunnel details.
- `local-development` for local env and run commands.
