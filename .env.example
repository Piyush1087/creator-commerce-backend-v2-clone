PORT=3000
STAGE=local
DATABASE_URL=postgresql://postgres:password@localhost:5432/thecreatorshop?schema=public

# Optional SST dev deploy override. Prod keeps using the SST Aurora resource.
DEV_DATABASE_URL=
