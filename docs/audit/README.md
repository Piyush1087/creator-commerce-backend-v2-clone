# Backend audits

Tracking notes from code/schema audits. These are **not** delete-now instructions.

Use this tree for future module audits:

```text
docs/audit/
  README.md
  campaigns/
  collaboration/
  <module>/
```

Rules:

- Audits capture gaps, dual stacks, and cleanup candidates.
- Do **not** delete or collapse schema/code until the relevant end-to-end journey is accepted.
- Prefer dated filenames so later audits can supersede earlier ones without rewriting history.
