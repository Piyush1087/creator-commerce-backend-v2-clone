While your current bridge specification defines the high-level data translations and mappings correctly, it is **not yet good enough for a developer to implement with 100% precision**.  
To build this without continuous back-and-forth, a engineer requires explicit **REST API endpoint contracts**, **exact transactional failure edge cases**, **explicit JSON validations**, and **raw SQL query strings for state updates**.  
Here is the production-grade, end-to-end **Comprehensive Developer Integration Document** to pass directly to your engineering team.

# **🛠️ SYSTEM INTEGRATION SPECIFICATION: BRAND CENTRE TO CAMPAIGN ENGINE**

## **1\. ARCHITECTURAL OVERVIEW & DATA FLOW**

This service handles asynchronous event payloads generated by **Tab 3: Campaign Planner** inside the Brand Centre and applies them to the **Campaign Workspace** execution engine.  
\[Brand Centre Planner\] ──(HTTP POST Event)──► \[Aura API Gateway Router\]  
                                                      │  
                                                      ▼  
\[Campaign Workspace DB\] ◄──(ACID Transaction)─── \[Translation Bridge\]

## **2\. REFACTOR SCHEMA DECREES (MANDATORY RUNTIME DATABASE MIGRATIONS)**

To avoid runtime casting exceptions during across-module data hand-offs, execute the following SQL migration on your production database cluster immediately.  
SQL  
\-- 1\. DROP OBSOLETE ONBOARDING ROUTING ENUM TO PREVENT INTER-MODULE DATA COLLISION  
DROP TYPE IF EXISTS brand\_industry\_routing\_enum CASCADE;

\-- 2\. CREATE UNIFIED DYNAMIC INDUSTRY ENUM MAPPED ACROSS ALL APPLICATION SHELLS  
CREATE TYPE industry\_sector\_enum AS ENUM (  
    'D2C\_ECOMMERCE',   
    'HEALTHCARE',   
    'AI\_SAAS',   
    'OFFLINE\_EXPERIENCES'  
);

\-- 3\. UNIFY CAMPAIGN STATE MATURITY ACROSS ENGINE CACHES  
DROP TYPE IF EXISTS campaign\_lifecycle\_maturity\_enum CASCADE;  
CREATE TYPE campaign\_lifecycle\_state\_enum AS ENUM (  
    'DRAFT',   
    'LIVE',   
    'PAUSED',   
    'COMPLETED',   
    'LIVE\_PENDING\_APPROVALS'  
);

## **3\. EDGE REST ENDPOINT INTERFACE DESIGN CONTRACTS**

### **3.1 Endpoint: Process Strategic Planner Signal**

* **Route:** POST /api/v1/orchestration/process-signal  
* **Content-Type:** application/json  
* **Authorization:** Bearer \<JWT\_WORKSPACE\_TOKEN\>

#### **Payload Body A: Launch Strategic Framework (Green Card Event)**

JSON  
{  
  "signal\_type": "LAUNCH\_NEW\_FRAMEWORK",  
  "brand\_id": "a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11",  
  "campaign\_name": "Aura Hydrating Summer Vibe",  
  "industry\_sector": "D2C\_ECOMMERCE",  
  "assigned\_macro\_objective": "PRODUCTION",  
  "raw\_budget\_expression": "$3500 per creator allocation for 10 creators",  
  "timeline\_expression": "June 30, 2026"  
}

#### **Payload Body B: Inject Asset Line (Amber Card Event)**

JSON  
{  
  "signal\_type": "INJECT\_ASSET\_LINE",  
  "campaign\_id": "f81d4fae-7dec-11d0-a765-00a0c91e6bf6",  
  "product\_name": "Milky Ceramide Cleanser",  
  "estimated\_base\_price": 28.00,  
  "raw\_strategic\_context": "Focus heavily on deep tissue absorption properties and barrier repair. Do not claim it cures eczema.",  
  "creative\_briefs": \[  
    {  
      "brief\_name": "Aesthetic Bathroom GRWM",  
      "deliverable\_type": "REEL\_VIDEO",  
      "compensation\_type": "BARTER"  
    }  
  \]  
}

#### **Payload Body C: Fast-Track Interrupt (Red Card Event)**

JSON  
{  
  "signal\_type": "FAST\_TRACK\_INTERRUPT",  
  "campaign\_id": "f81d4fae-7dec-11d0-a765-00a0c91e6bf6",  
  "target\_entity\_type": "BRIEF",  
  "target\_entity\_uuid": "c3b9a710-1c4b-4a5d-b062-819cf1e2bf63"  
}

#### **Common Response Statuses**

* 201 Created: Signal successfully converted and written into the live execution database.  
* 422 Unprocessable Entity: Input strings could not be evaluated by the AI Parsing Engine.  
* 409 Conflict: Business logic violation (e.g., attempting to update an asset while live collaborations are locked).

## **4\. DETAILED DATA CONVERSION & INVARIANT COMPLIANCE LOGIC**

Engineers must adhere strictly to the following computation rules when transforming raw planner parameters into structured database targets.  
TypeScript  
import { z } from "zod";

/\*\*  
 \* INVARIANT RULE 4.1: Financial Budgets Mathematical Divider  
 \* Logic: Reads raw textual budget descriptions and parses integers.  
 \* Formula:   
 \* Master Allocation \= Upper Target Bound \* Total Targeted Creator Pool  
 \* Logistics Cap (product\_sub\_ceiling\_cap) \= Master Allocation \* 0.15  
 \*/  
export function calculateSplittingBudgets(rawExpression: string): { masterBudget: number; subCeilingCap: number } {  
  const numbers \= rawExpression.match(/\\d+/g)?.map(Number) || \[0, 0\];  
  if (numbers.length \< 2\) {  
    throw new Error("UNABLE\_TO\_PARSE\_FINANCIAL\_EXPRESSION");  
  }  
    
  const ratePerCreator \= numbers\[0\];   
  const totalCreators \= numbers\[1\];  
    
  const masterBudget \= ratePerCreator \* totalCreators;  
  const subCeilingCap \= Number((masterBudget \* 0.15).toFixed(2));  
    
  return { masterBudget, subCeilingCap };  
}

/\*\*  
 \* INVARIANT RULE 4.2: Timeline Parser Engine Mapping  
 \*/  
export function parseTimelineBounds(expression: string): { type: "EVERGREEN" | "FIXED\_DATE" | "DYNAMIC"; date?: Date; offset?: number } {  
  if (\!expression || expression.toLowerCase().includes("evergreen")) {  
    return { type: "EVERGREEN" };  
  }  
    
  const dateParsed \= Date.parse(expression);  
  if (\!isNaN(dateParsed)) {  
    return { type: "FIXED\_DATE", date: new Date(dateParsed) };  
  }  
    
  const daysMatch \= expression.match(/(\\d+)\\s\*days/i);  
  if (daysMatch) {  
    return { type: "DYNAMIC", offset: parseInt(daysMatch\[1\], 10\) };  
  }  
    
  return { type: "EVERGREEN" };  
}

## **5\. RE-COMPILATION CRITERIA BY VERTICAL SECTOR**

When creating row records within campaign\_products\_catalog, the JSONB schema layout configurations must change layout dynamically based on the checked value of industry\_sector:

### **5.1 D2C\_ECOMMERCE Mapping Matrix**

* positioning\_usps**:** Extract arrays targeting immediate sensory benefits or unique physical ingredients.  
* compliance\_guardrails**:** Inject explicit FTC guidelines regarding clear, audible platform endorsements.  
* fulfillment\_metadata**:** Set standard payload values to designate item ownership rules upon verification completion.

### **5.2 HEALTHCARE Mapping Matrix**

* positioning\_usps**:** Extract clinical trials parameters, expert testing confirmations, and skin-safety indicators.  
* compliance\_guardrails**:** Hardcode mandatory requirements for visible safety warnings and clear statements regarding unapproved off-label use.  
* fulfillment\_metadata**:** Restrict return routes and require valid compliance tracking parameters.

### **5.3 AI\_SAAS Mapping Matrix**

* positioning\_usps**:** Collect operational software values, cloud infrastructure metrics, and core API capabilities.  
* compliance\_guardrails**:** Enforce explicit security rules to prevent sharing internal connection environments or raw data records in public facing content.  
* fulfillment\_metadata**:** Generate an access pool mapping directly to development sandbox workspace environments.

### **5.4 OFFLINE\_EXPERIENCES Mapping Matrix**

* positioning\_usps**:** Identify geographic destinations, environment vibes, and venue locations.  
* compliance\_guardrails**:** Require clear, signed appearance waivers for any background individuals captured in footage.  
* fulfillment\_metadata**:** Set calendar booking expiration limits and log physical street address coordinate references.

## **6\. TRANSACTIONAL ERROR BOUNDARIES & WORKFLOW CODES**

To prevent bad data from corrupting active workflow contracts, the engine must block updates and throw explicit error responses when business safety logic rules are violated:  
                 \[ ENGINE DISPATCH UPDATE \]  
                              │  
          Is COUNT(active\_collab\_id) \> 0 for Product?  
                              ├───► YES ──► Abort Transaction (Code 551\)  
                              ▼  
          Is COUNT(applicant\_id) \> 0 for Creative Brief?  
                              ├───► YES ──► Lock Guidelines (Code 552\)  
                              ▼  
                   Execute Database Commit

### **6.1 Product Mutation Lock (Code 551\)**

* **Trigger Condition:** Any pipeline update signal tries to modify or remove a row inside campaign\_products\_catalog where an active collaboration tracking line is attached (COUNT(active\_collab\_id) \> 0).  
* **Database Level Enforcement Action:** System issues an internal rollback command and updates the frontend state:

JSON  
{  
  "error\_code": "PRODUCT\_MUTATION\_LOCKED\_LIVE\_COLLAB",  
  "http\_status": 409,  
  "developer\_diagnostic\_message": "Mutation aborted. Active creator records are currently tied to this product SKU line. Modification is blocked to protect fulfillment logs.",  
  "target\_lock\_count": 3  
}

### **6.2 Creative Brief Freeze Rule (Code 552\)**

* **Trigger Condition:** An inbound signal targets a campaign\_strategy\_briefs row layout file while active applications are processing (COUNT(applicant\_id) \> 0).  
* **Database Level Enforcement Action:** The API blocks structural string modifications, updates the layout fields to a read-only state, and returns a tracking notice payload:

JSON  
{  
  "error\_code": "CREATIVE\_BRIEF\_BOUNDS\_FROZEN",  
  "http\_status": 422,  
  "developer\_diagnostic\_message": "Creative direction cannot be altered because creator applications have already been submitted against these active parameters.",  
  "fallback\_ui\_action": "SET\_OPACITY\_40\_DISABLE\_TOGGLES"  
}

## **7\. AI ENGINE PROMPT SCHEMAS (DETERMINISTIC COMPREHENSIVE OUTPUTS)**

To ensure the AI engine generates structured JSON payloads without erratic markdown code wrapper formatting, implement the following orchestration prompt designs directly into your LLM runtime invocation code:

### **7.1 Production Invariant Execution Prompt: Creative Brief Synthesis**

SYSTEM DIRECTIVE:  
You are a deterministic, type-safe database translation adapter. Your task is to process unstructured creative briefs from a campaign planner and output a cleanly structured, valid JSON object that matches the target schema definition perfectly.

INPUT PARAMETERS TO RESOLVE:  
\- Raw Intent Vibe: "{{raw\_strategic\_context}}"  
\- Delivery Frame Target: "{{deliverable\_type}}"  
\- Verified Domain Sector: "{{industry\_sector}}"

EXECUTION COMPLIANCE MANDATES:  
1\. Output ONLY a valid JSON structure. Do not wrap the output in markdown code blocks like \`\`\`json ... \`\`\`, and do not provide an introductory preamble or concluding explanation.  
2\. Ensure every single key-value slot is completely generated with explicit context strings; do not truncate lines or output placeholder labels like "..." or "TODO".  
3\. Apply industry sector logic variations to match the target track:  
   \- For D2C\_ECOMMERCE: Output clear product feature hooks and explicit instructions to include legible disclosure tags.  
   \- For HEALTHCARE: Enforce a balanced, professional tone that stays within certified skin-safe claims, introduces placeholders for clinical data points, and prohibits making medical treatment claims.  
   \- For AI\_SAAS: Focus the content on capturing smooth software dashboard screen-recordings and emphasize data privacy and workspace security parameters.  
   \- For OFFLINE\_EXPERIENCES: Define environmental visual targets, map unique spatial transitions, and outline crowd control safety guidelines.

OUTPUT INTERFACE DEFINITION JSON TEMPLATE:  
{  
  "brief\_name": "Provide a high-converting operational title matching the campaign theme.",  
  "content\_theme": "A clear, 3 to 5 word aesthetic focus statement.",  
  "description\_text": "A comprehensive paragraph outlining the creative narrative guidelines.",  
  "hook\_options": \[  
    "High-impact hook variation 1 (optimized for the target channel platform)",  
    "High-impact hook variation 2 (optimized for the target channel platform)"  
  \],  
  "b\_roll\_requirements": "Detailed, step-by-step instructions for capturing supporting visual footage.",  
  "audio\_strategy": "Specific instructions regarding voiceovers, background audio, or audio styles.",  
  "tone\_of\_voice": "3 explicit adjectives specifying the personality of the output content.",  
  "lighting\_requirement": "Detailed lighting specifications tailored to the target setting.",  
  "background\_setting": "Specific instructions for arranging and managing the background scene.",  
  "post\_caption\_template": "A ready-to-use template for post captions, including placeholder tokens like \[Link\] or \[Discount\].",  
  "required\_hashtags": \["\#MustBeginWithHash", "\#BrandPartnerSpecificTag"\],  
  "dos\_checklist": \[  
    "Core instruction 1 for verification success",  
    "Core instruction 2 for verification success"  
  \],  
  "donts\_restrictions": \[  
    "Explicit regulatory or visual restriction 1",  
    "Explicit regulatory or visual restriction 2"  
  \]  
}

### **7.2 Production Invariant Execution Prompt: Catalog Polymorphic Normalizer**

SYSTEM DIRECTIVE:  
You are an inventory parsing engine. Your job is to extract high-level product definitions and build out detailed, multi-industry catalog records.

INPUT STRINGS TO PARSE:  
\- Text: "{{product\_name}} \- {{raw\_strategic\_context}}"  
\- Active Domain Node: "{{industry\_sector}}"

DATA COMPLIANCE PARSING RULES:  
Transform the raw data input strings into an un-wrapped, production-ready JSON map matching the schema definition below. You must customize your extracted fields based on the targeted active domain:  
\- For D2C\_ECOMMERCE: Identify retail selling points and product preservation rules.  
\- For HEALTHCARE: Extract authorized product properties and regulatory constraints.  
\- For AI\_SAAS: Map software capabilities and data handling rules.  
\- For OFFLINE\_EXPERIENCES: Track geographic properties and booking liability criteria.

OUTPUT VALIDATION STRUCTURE:  
{  
  "extracted\_product\_name": "Cleaned up asset title string",  
  "estimated\_base\_price": 0.00,  
  "positioning\_usps": \[  
    "Extracted product value proposition claim statement 1",  
    "Extracted product value proposition claim statement 2"  
  \],  
  "compliance\_guardrails": {  
    "regulatory\_policy\_rules": "Detailed data processing restrictions and verification rules.",  
    "strict\_do\_not\_say\_tokens": \["unauthorized\_term\_alpha", "unauthorized\_term\_beta"\]  
  },  
  "fulfillment\_metadata": {  
    "condition\_clause": "Specific logistics handling terms and sample tracking rules."  
  }  
}

## **8\. CORE SQL QUERIES FOR DATA SYNCHRONIZATION**

Provide these optimized SQL queries directly to your database engineer to facilitate smooth data integration across schemas.

### **8.1 Inbound Sync Query: Create Parent Campaign Frame**

SQL  
INSERT INTO campaigns\_execution (  
    campaign\_id,   
    brand\_id,   
    campaign\_name,   
    industry\_sector,   
    lifecycle\_state,   
    assigned\_macro\_objective,   
    master\_budget\_limit,  
    product\_sub\_ceiling\_cap,  
    deadline\_type,  
    deadline\_date,  
    dynamic\_days\_offset  
) VALUES (  
    $1, \-- campaign\_id (UUID)  
    $2, \-- brand\_id (UUID)  
    TRIM($3), \-- campaign\_name  
    $4::industry\_sector\_enum,   
    $5::campaign\_lifecycle\_state\_enum,   
    $6::macro\_objective\_enum,   
    $7, \-- master\_budget\_limit (DECIMAL)  
    $8, \-- product\_sub\_ceiling\_cap (DECIMAL)  
    $9::deadline\_type\_enum,  
    $10, \-- deadline\_date (DATE or NULL)  
    $11  \-- dynamic\_days\_offset (INT or NULL)  
) RETURNING campaign\_id;

### **8.2 Safe Mutation Query: Verify Pipeline Before Modifying Product SKU**

SQL  
\-- Enforce a check on downstream active dependencies before executing database mutation updates  
SELECT COUNT(c.collab\_id) AS active\_dependencies  
FROM campaign\_products\_catalog p  
LEFT JOIN campaign\_strategy\_briefs b ON p.product\_id \= b.product\_id  
LEFT JOIN creator\_collaborations\_ledger c ON b.brief\_id \= c.brief\_id  
WHERE p.product\_id \= $1  
GROUP BY p.product\_id;

\-- Developer Instruction: If query result returns a count \> 0, abort the operation immediately and throw Error Code 551\.

### **8.3 State Sync Query: Automated Cascading Freeze Trigger**

SQL  
\-- Manually execute a safety freeze across downstream briefs when an injection source product line is paused  
UPDATE campaign\_strategy\_briefs  
SET is\_active \= FALSE  
WHERE product\_id \= $1   
  AND is\_active \= TRUE  
RETURNING brief\_id, brief\_name;

