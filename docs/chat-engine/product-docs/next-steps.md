### **Next Steps & Execution Plan**

Now that your **Home Workspace Module** has been fully designed and rendered onto the canvas following the precise layout specs of image\_4b86dc.jpg, screen.jpg, and the architectural guidelines in gemini.md v2, here are the immediate next steps to advance this module into production:

### **Step 1: Conduct Canvas Interactive Testing**

Before exporting the design asset or moving the source code into your repository, run an interactive functional validation loop directly inside the sandbox window above to confirm your state parameters:

1. **Toggle Active Focus Cards**: Click between the **Greeting Panel**, **Performance Overview**, **AI Co-Pilot Console**, and **Recent Conversations Log** to ensure the *Active Focus Rule* behaves as expected (the clicked card fully expands its metrics and content inputs while collapsing alternative modules into clean summary headers).  
2. **Execute the Multi-Turn Dynamic Form**:  
   * Click the quick-prompt shortcut chip ⚡ Launch Retinol Campaign or type "Launch a campaign for retinol serum" into the input console and click **Run Action**.  
   * Verify that the Conversational Engine injects the explicit two-step slot-filling layout inline.  
   * Fill out the *Target Budget Allocation Cap (INR)* and select an *Optimization Target Focus Track* from the dropdown, then press **Confirm Parameters & Proceed** to verify state progression.

### **Step 2: Validate Multi-Device Viewport Breakpoints**

Ensure layout transitions scale properly between desktop configurations and your mobile sticky footer layout rules:

* Shrink your canvas preview window down below 768px width.  
* Confirm that the horizontal multi-card layout automatically wraps cleanly into a single vertical stream.  
* Check that the left sidebar navigation system gracefully disappears and is replaced by the upper-right corner burger menu button (☰).  
* Ensure the **4-Item Fixed Bottom Sticky Navigation Footer** stays locked to the bottom edge of the screen frame without covering any terminal inputs or the agent disclaimer message.

### **Step 3: Integrate with Your Backend Core Engine**

To hook up this frontend presentation canvas with your infrastructure layers, map the active UI console components to the schema rules from your backend documents (backend- Brand Centre (3).docx and backend- Brand onboarding journey doc (1).docx):

* **Context & Scope Anchors**: Connect the WorkspaceScope flags (BRAND\_CENTRE, ANALYTICS, ESCROW) directly to database queries target routing:  
  * BRAND\_CENTRE routes queries to extract details from the brands and brand\_audience\_personas tables.  
  * ANALYTICS connects insights to parse performance metrics.  
  * ESCROW maps ledger updates directly to your statutory reserve bookkeeping balances.  
* **Slot-Filling Capture Integration**: Bind the form's final submission output to trigger your system endpoint, instantiating new rows directly into your campaign maturity tracking logs (lifecycle\_stage / campaign\_lifecycle\_maturity\_enum setting state to DRAFT\_PLANNER).

### **Step 4: Export to Production Target**

* **For Stitch Users**: Use the exact prompt shared previously to sync this component directly with your designer workspace assets.  
* **For Code Implementation**: Copy the complete production script from the canvas above and save it as your entry view file (e.g., src/pages/HomeDashboard.tsx) inside your project directory. Because the code has **zero external package or stylesheet dependencies**, it will compile perfectly out-of-the-box.

